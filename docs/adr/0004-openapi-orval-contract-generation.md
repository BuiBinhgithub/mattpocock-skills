# OpenAPI and Orval Code Generation for React Query

NestJS Swagger serves as the single source of truth for API specifications. We use Orval to automatically generate TypeScript models and TanStack React Query hooks for `apps/web`. Hand-crafting API fetching code and query hooks is prohibited to eliminate endpoint drift and duplicate typing.
