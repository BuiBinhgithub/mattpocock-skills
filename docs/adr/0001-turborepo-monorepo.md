# Turborepo Monorepo for Next.js and NestJS

We adopt a Turborepo monorepo structure containing `apps/web` (Next.js), `apps/api` (NestJS), and `packages/contracts` (shared schemas and DTOs). This enables end-to-end type safety across client and server boundaries, unified CI/CD pipelines, and atomic cross-stack pull requests.
